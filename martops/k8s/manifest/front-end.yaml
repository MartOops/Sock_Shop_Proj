apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-end
  namespace: sock-shop-mops
spec:
  replicas: 3
  selector:
    matchLabels:
      app: front-end
  template:
    metadata:
      labels:
        app: front-end
    spec:
      containers:
        - name: frontend
          image: weaveworksdemos/front-end:0.3.12
          ports:
            - containerPort: 8079
